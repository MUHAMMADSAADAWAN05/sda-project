

## Plan: Remove "Switch Role" Button

Remove the floating "Switch Role" button that appears on all pages.

### Changes
1. **`src/App.tsx`** — Remove the `<BackToWelcome />` component and its import from the layout.

That's it — one file, two lines removed.

